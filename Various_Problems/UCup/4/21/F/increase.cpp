#include <bits/stdc++.h>

using namespace std;

int N;
vector<int> A, B;

int main(void) {
    scanf("%d", &N);

    A.resize(N);
    B.resize(N);

    for (int i = 0; i < N; i++) {
        scanf("%d", &A[i]);
    }
    for (int i = 0; i < N; i++) {
        scanf("%d", &B[i]);
    }
    
}