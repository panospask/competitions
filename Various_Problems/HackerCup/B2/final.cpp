#include <bits/stdc++.h>

using namespace std;

typedef long long ll;

const int MOD = 1e9 + 7;

ll N, A, B;

void solve(void) {
    scanf("%lld %lld %lld", &N, &A, &B);

    // Find divisors of i
    for (int i = 2; (ll)i * i <= B; i++) {
        ;
    }
}

int main(void) {
    int t;
    scanf("%d", &t);

    for (int i = 0; i < t; i++) {
        printf("Case #%d: ", i);
        solve();
    }
}