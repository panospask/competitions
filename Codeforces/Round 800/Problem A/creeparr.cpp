#include <bits/stdc++.h>

using namespace std;

int arr[200];
int a, b;

void insert_min(int num, int type)
{
    for (int i = 0; i < a + b; i++) {
        
    }
}

int main(void)
{
    int t;
    scanf("%d", &t);

    for (int x = 0; x < t; x++) {
        scanf("%d %d", &a, &b);

        int ins_b = (a + b) / b;
        int ins_a = (a + b) / a;
        for (int i = 0; i < a + b; i++) {
            ;
        }

    }
}