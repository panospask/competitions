#include <bits/stdc++.h>

using namespace std;

int N, M;
vector<int> a;

int main(void) {
    scanf("%d %d", &N, &M);

    a.resize(N);
    for (int i = 0; i < N; i++) {
        scanf("%d", &a[i]);
    }
}