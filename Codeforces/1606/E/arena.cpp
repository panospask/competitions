#include <bits/stdc++.h>

using namespace std;

typedef long long ll;

const int MOD = 998244353;

int N, X;
ll ans = 0;

int main(void)
{
    scanf("%d %d", &N, &X);

    for (int maxh = 1; maxh <= X; maxh++) {
        for (int winners = 2; winners <= N; winners++) {
            ;
        }
    }
}